#include <iostream>

using namespace std;

int main() { 
    int a, b;
    cin >> a >> b;
    // cout << a << b;
    int area = a * b;
    int l = a * 2 + b * 2;
    cout << area << " " << l << endl;
    return 0;
}